# FileCache Test setup

A minimal spring-boot application testing the [FileCache](../file-cache-impl).

Run it from the parent folder:

```
cd ..
mvn -pl test-setup -am spring-boot:run
```

or from an IDE of your choice...